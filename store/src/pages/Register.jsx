import React,{useState} from 'react'; import {Link,useNavigate} from 'react-router-dom'; import {api} from '../api'; import Status from '../components/Status';
export default function Register(){const nav=useNavigate(); const [form,setForm]=useState({name:'',email:'',password:''}),[error,setError]=useState(''),[saving,setSaving]=useState(false); const submit=async e=>{e.preventDefault();setSaving(true);setError('');try{await api('/auth/register',{method:'POST',body:JSON.stringify(form)});nav('/login')}catch(e){setError(e.message)}finally{setSaving(false)}};return <div className="auth-shell"><div className="auth-card"><Link to="/" className="brand auth-brand"><span className="brand-mark">✦</span>KriyaKita</Link><span className="eyebrow">AKUN BARU</span><h1>Mulai berbelanja.</h1><p className="form-muted">Buat akun untuk menyimpan keranjang dan riwayat pesanan.</p><form className="form-card-inner" onSubmit={submit}><label>Nama lengkap<input required minLength="3" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Nama kamu"/></label><label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="kamu@email.com"/></label><label>Password<input required minLength="6" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Minimal 6 karakter"/></label>{error&&<Status type="error">{error}</Status>}<button className="btn btn-primary btn-wide" disabled={saving}>{saving?'Membuat akun...':'Buat akun'}</button></form><p>Sudah punya akun? <Link to="/login">Masuk</Link></p><Link className="back-link" to="/">← Kembali ke toko</Link></div></div>}
