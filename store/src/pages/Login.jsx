import React,{useState} from 'react'; import {Link,useLocation,useNavigate} from 'react-router-dom'; import {api} from '../api'; import Status from '../components/Status';
export default function Login(){const nav=useNavigate(),loc=useLocation(); const [form,setForm]=useState({login:'',password:''}),[error,setError]=useState(''),[saving,setSaving]=useState(false); const submit=async e=>{e.preventDefault();setSaving(true);setError('');try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify(form)});localStorage.setItem('token',d.token);localStorage.setItem('user',JSON.stringify(d.user));window.dispatchEvent(new Event('authchange'));nav(loc.state?.from||'/')}catch(e){setError(e.message)}finally{setSaving(false)}};return <div className="auth-shell"><div className="auth-card"><Link to="/" className="brand auth-brand"><span className="brand-mark">✦</span>KriyaKita</Link><span className="eyebrow">SELAMAT DATANG</span><h1>Masuk ke Kriya Kita.</h1><p className="form-muted">Gunakan akunmu untuk checkout dan melihat pesanan.</p><form className="form-card-inner" onSubmit={submit}><label>Email atau nama pengguna<input autoFocus required value={form.login} onChange={e=>setForm({...form,login:e.target.value})} placeholder="nama atau email"/></label><label>Password<input required type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="••••••••"/></label>{error&&<Status type="error">{error}</Status>}<button className="btn btn-primary btn-wide" disabled={saving}>{saving?'Memeriksa...':'Masuk'}</button></form><p>Belum punya akun? <Link to="/register">Daftar sekarang</Link></p><Link className="back-link" to="/"><span>←</span> Kembali ke toko</Link></div></div>}
