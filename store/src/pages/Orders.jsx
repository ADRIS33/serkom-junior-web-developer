import React,{useEffect,useState} from 'react'; import {Link} from 'react-router-dom'; import {api,rupiah} from '../api'; import Status from '../components/Status';
const labels={pending:'Menunggu',processing:'Diproses',shipped:'Dikirim',completed:'Selesai',cancelled:'Dibatalkan'};
export default function Orders(){const [orders,setOrders]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(''); useEffect(()=>{if(!localStorage.getItem('token')){setLoading(false);return}api('/orders').then(setOrders).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]); if(!localStorage.getItem('token'))return <div className="container page-shell narrow"><div className="empty-card"><h1>Belum masuk.</h1><p>Masuk untuk melihat riwayat pesanan.</p><Link className="btn btn-primary" to="/login">Masuk</Link></div></div>; return <div className="container page-shell"><div className="page-intro"><div><span className="eyebrow">RIWAYAT PESANAN</span><h1>Pesanan saya.</h1><p>Pantau status karya yang sudah kamu pesan.</p></div></div>{loading?<Status>Memuat pesanan...</Status>:error?<Status type="error">{error}</Status>:!orders.length?<div className="empty-card"><h2>Belum ada pesanan.</h2><Link className="btn btn-primary" to="/products">Lihat koleksi</Link></div>:<div className="orders-list">{orders.map(o=><article className="order-card" key={o.id}><div className="order-head"><div><span>ORDER #{String(o.id).padStart(4,'0')}</span><small>{new Date(o.created_at).toLocaleString('id-ID')}</small></div><b className={`badge badge-${o.status}`}>{labels[o.status]}</b></div><div className="order-items">{o.items.map(i=><div key={i.id}><span>{i.product_name} × {i.quantity}</span><b>{rupiah(Number(i.price)*Number(i.quantity))}</b></div>)}</div><div className="order-foot"><span>{o.payment_method==='cod'?'COD':o.payment_method==='ewallet'?'E-wallet':'Transfer bank'}</span><strong>{rupiah(o.total)}</strong></div></article>)}</div>}</div>}
