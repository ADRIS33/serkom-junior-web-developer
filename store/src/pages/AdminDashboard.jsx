import React,{useEffect,useState} from 'react'; import {Link,useNavigate} from 'react-router-dom'; import {api,rupiah,getUser,logout} from '../api'; import Status from '../components/Status';
export default function AdminDashboard(){const nav=useNavigate(),user=getUser(); const [stats,setStats]=useState(null),[error,setError]=useState(''); useEffect(()=>{if(user?.role!=='admin'){nav('/');return}api('/admin/dashboard').then(setStats).catch(e=>setError(e.message))},[nav,user]); const out=()=>{logout();window.dispatchEvent(new Event('authchange'));nav('/')}; if(user?.role!=='admin')return null; return <div className="container page-shell"><div className="admin-head"><div><span className="eyebrow">ADMINISTRATOR</span><h1>Ruang kendali.</h1><p>Kelola katalog dan pesanan Kriya Kita.</p></div><button className="btn btn-secondary" onClick={out}>Keluar admin</button></div>{error&&<Status type="error">{error}</Status>}{stats&&<div className="stats-grid"><div><span>Produk</span><strong>{stats.products}</strong></div><div><span>Pelanggan</span><strong>{stats.users}</strong></div><div><span>Pesanan</span><strong>{stats.orders}</strong></div><div><span>Omzet tercatat</span><strong>{rupiah(stats.revenue)}</strong></div></div>}<div className="admin-links"><Link to="/admin/products"><span>01</span><div><b>Kelola produk</b><small>Tambah, edit, hapus dan atur stok.</small></div><span>→</span></Link><Link to="/admin/orders"><span>02</span><div><b>Kelola pesanan</b><small>Perbarui status pesanan pelanggan.</small></div><span>→</span></Link><Link to="/products"><span>03</span><div><b>Lihat toko</b><small>Periksa tampilan katalog dari sisi pelanggan.</small></div><span>→</span></Link></div></div>}
